import React from 'react'

function Skills({ fbend }) {
    console.log(fbend)
    return (

        <section>
            <li >
                {fbend}
            </li>
        </section>

    )
}

export default Skills;